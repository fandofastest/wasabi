import validateTag from '../_helper/validateTag'
import builderString from '../../src/utils/tagBuilder/builder.string'

describe('builder.string', () => {
  it('should create string tag', () => {
    const expected = [{ name: 'my name', type: 'CONTAINS' }]
    const results = builderString({ name: 'my name' }, 'name')

    validateTag(expected, results)
  })
})